Project: ml_server

	Mokonzi: A simplified http server that implemets a basic GET request using a boss-worker multi-threaded design.

Author: Michael Landes

	This code is for CS 8803 (GOS) GaTech Fall 2012
	There are no copywrights, no usage restrictions, and no guarentees :)

To Build:

	./make bin

Other Make Commands:

	./make clean


To Run:

	./bin/server

usage: ./bin/server [-p port] [-r root_directory] [-w workers]
 
 	[-p port] : valid (1-65535) port on which to listen (default is 51115)
 	[-r root_directory] : directory to serve (default is ./)
 	[-w workers] : a reasonable (1-50) number of workers to use (default is 5)

